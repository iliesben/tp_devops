pipeline {
    agent any 
    stages {
        stage('build') { 
            steps {
                sh "ls -la"
            }
        }
        stage('Test') { 
            steps {
                // sh "cd NodeJs_api/"
                sh "ls -la | grep server "
            }
        }
        stage('Deploy SIUUUUUUUUUUUUUU') { 
            steps {
                // sh "cd NodeJs_api/"
                sh "echo deploiement réussir"
            }
        }
    }
}
